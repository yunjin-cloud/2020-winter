def yunjin_chu()
  return 'a'
end

def malatang_tang()
  return 'B'
end

puts(yunjin_chu())

#루비의 모듈은 파일로 분리할 수도, 분리하지 않을 수도 있음.
#파이썬의 모듈은 파일로 분리된다는 특성 가짐.

#모듈을 만들고 싶으면 module이라는 키워드를 쓰면 됨
#모듈 이름은 반드시 대문자로 시작해야 함
